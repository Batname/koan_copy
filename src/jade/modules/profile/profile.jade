.col-md-4
  img.img-responsive.profile-image.img-circle(ng-src='{{user.picture}}', width='100px', height='100px')
.col-md-8
  h4 {{user.name}}
  table.table.table-striped
    tr
      td
        strong id:
      td {{user.id}}
    tr
      td
        strong email:
      td
        a(href='mailto:{{user.email}}') {{user.email}}
style(type='text/css').
  /* Layout */
  .profile-image {
  width: 140px;
  height: 140px;
  margin-top: 10px;
  margin-left: 10px;
  }
